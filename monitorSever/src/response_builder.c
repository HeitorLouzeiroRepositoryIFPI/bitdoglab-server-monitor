#include "response_builder.h"
#include <stdio.h>

const char* construirRespostaHttp(const char* conteudo, int codigoStatus) {
    // Constrói e retorna uma resposta HTTP
    return "Resposta HTTP";
}
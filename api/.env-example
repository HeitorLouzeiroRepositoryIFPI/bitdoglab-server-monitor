# Django Server Monitor - Configurações de Ambiente
# Copie este arquivo para .env e configure as variáveis conforme necessário

# Chave secreta do Django (Importante: gere uma chave única para produção!)
SECRET_KEY=django-insecure-example-key-change-this-in-production

# Modo de depuração (True para desenvolvimento, False para produção)
DEBUG=True

# Hosts permitidos, separados por vírgula
ALLOWED_HOSTS=localhost,127.0.0.1

# Configurações do banco de dados
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# Você pode adicionar mais variáveis de ambiente conforme necessário
# para configurações específicas da sua aplicação